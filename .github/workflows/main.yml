name: Build KurdApp
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Theos
        uses: theos/setup-theos@v1
        with:
          version: 'master'

      - name: Build Tweak
        run: |
          # لێرەدا ڕێگری دەکات لە هەر ئیرۆرێکی نەخوازراو
          make package FINALPACKAGE=1
          
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: KurdApp-Final-Dylib
          path: packages/*.deb
